[advisories]
# quick-xml is pulled transitively through bevy 0.15 -> winit 0.30 ->
# smithay-client-toolkit -> wayland-scanner. The latest compatible
# wayland-scanner line only reaches quick-xml 0.39.4, while these advisories
# require quick-xml >=0.41.0. Keep this narrow until the Bevy/winit migration
# in Dependabot #14 can move the Wayland stack past the constraint.
ignore = [
  "RUSTSEC-2026-0194",
  "RUSTSEC-2026-0195",
]
